# PowerShell scripts require CRLF for Windows PowerShell 5.x compatibility.
# PS 5.x fails to parse functions/scripts saved with LF line endings.
*.ps1 text eol=crlf

# Shell scripts use LF (Unix convention).
*.sh text eol=lf
